# HoodedHeroes Obsidian vault

Open this `docs/vault` directory as an Obsidian vault. It intentionally uses plain Markdown and relative wikilinks, so Git remains the source of truth and the knowledge base remains useful without Obsidian.

Start at [[00 - HoodedHeroes Command Index]]. Local workspace state, plugins, caches, and personal UI settings should not be committed.

